# ПетроЭксперт - Центр независимой экспертизы

Современный веб-сайт для центра независимой профессиональной экспертизы "ПетроЭксперт".

## 🚀 Технологии

### Frontend
- HTML5
- CSS3 (Custom Properties, Flexbox, Grid)
- Vanilla JavaScript (ES6+)
- Fetch API

### Backend
- Node.js
- Express
- MongoDB (Mongoose)
- JWT для аутентификации
- Nodemailer для отправки email

## 📁 Структура проекта

```
PetroExpert/
├── frontend/          # HTML/CSS/JS приложение
│   ├── index.html     # Главная страница
│   ├── pages/         # HTML страницы
│   ├── css/           # CSS стили
│   ├── js/            # JavaScript файлы
│   └── package.json
├── backend/           # Node.js API
│   ├── controllers/   # Контроллеры
│   ├── models/        # Модели данных
│   ├── routes/        # Маршруты API
│   ├── middleware/    # Middleware
│   └── server.js
├── .gitignore         # Git ignore файл
└── README.md          # Документация
```

## 🛠️ Установка и запуск на новом ПК

### Предварительные требования

- Node.js (версия 18 или выше)
- npm (версия 9 или выше)
- MongoDB (локально или MongoDB Atlas) - опционально, если используете backend функциональность

### Быстрый старт

1. **Клонируйте репозиторий** (если используете git):
```bash
git clone <repository-url>
cd PetroExpert
```

2. **Установите зависимости для всего проекта:**
```bash
npm run install:all
```

Эта команда установит зависимости для:
- Корневого проекта (concurrently)
- Frontend (http-server)
- Backend (express, mongoose, и т.д.)

3. **Настройте переменные окружения (опционально):**

Если вы используете backend функциональность, создайте файл `backend/.env`:

```bash
cd backend
# Создайте .env файл с переменными:
NODE_ENV=development
PORT=5000
MONGODB_URI=mongodb://localhost:27017/petroexpert
JWT_SECRET=your-secret-key-here
JWT_EXPIRE=7d
FRONTEND_URL=http://localhost:3000
```

**Примечание:** Если вы только просматриваете сайт (frontend), переменные окружения не обязательны.

4. **Запустите приложение:**

Из корня проекта выполните:
```bash
npm run dev
```

или

```bash
npm start
```

Обе команды запустят:
- **Backend** на порту `5000` (http://localhost:5000)
- **Frontend** на порту `3000` (http://localhost:3000)

Приложение автоматически откроется в браузере.

### Альтернативный запуск (по отдельности)

Если нужно запустить только одну часть:

**Backend (порт 5000):**
```bash
cd backend
npm run dev
```

**Frontend (порт 3000):**
```bash
cd frontend
npm start
```

## 📝 Основные функции

### Для пользователей:
- ✅ Каталог услуг (экспертизы, оценки, лабораторные исследования)
- ✅ Личный кабинет с историей заказов
- ✅ Корзина для оформления заказов
- ✅ Избранное для сохранения услуг
- ✅ Сравнение услуг
- ✅ Онлайн-заявки и форма обратной связи
- ✅ Заказ обратного звонка
- ✅ Блог с полезными статьями
- ✅ Многофилиальность (4 города: СПб, Москва, Новгород, Петрозаводск)

### Для администраторов:
- ✅ Управление услугами
- ✅ Управление заказами
- ✅ Управление блогом
- ✅ Просмотр пользователей

## 🎨 Дизайн

Современный, чистый дизайн с использованием:
- Адаптивная верстка (mobile-first)
- Плавные анимации и переходы
- Интуитивный пользовательский интерфейс
- Кастомный шрифт Onest

## 🔧 Разработка

### API Endpoints

- `GET /api/health` - Проверка работы API
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/profile` - Профиль пользователя
- `GET /api/services` - Список услуг
- `GET /api/services/:id` - Детали услуги
- `POST /api/orders` - Создать заказ
- `GET /api/orders` - Список заказов
- `GET /api/blog` - Список статей
- `POST /api/contact/form` - Отправить форму
- `POST /api/contact/callback` - Заказать звонок

### Переменные окружения (Backend)

**Backend (.env):**
- `NODE_ENV` - Режим работы (development/production)
- `PORT` - Порт сервера (по умолчанию 5000)
- `MONGODB_URI` - Строка подключения к MongoDB
- `JWT_SECRET` - Секретный ключ для JWT
- `JWT_EXPIRE` - Время жизни токена
- `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASSWORD` - Настройки email
- `FRONTEND_URL` - URL фронтенда для CORS

## 📧 Контакты

**ООО "Центр независимой профессиональной экспертизы "ПетроЭксперт"**

- Email: info5@petroexpert.ru
- Телефон: 8 (812) 331-81-80
- Адрес: г. Санкт-Петербург, ул. Рубинштейна, 34

## 📄 Лицензия

Все права защищены © 2025 ПетроЭксперт
